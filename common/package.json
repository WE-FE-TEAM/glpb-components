{
  "name": "glpb-components-common",
  "version": "1.0.17",
  "description": "common utils/base component of glpb components",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && npm run assets && babel src -d dist && node-sass ./src -o dist ",
    "assets": "copy './src/**/*.?(png|jpg|webp|jpeg|gif)' ./dist/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-plugin-transform-runtime": "^6.12.0",
    "babel-preset-es2015": "^6.13.2",
    "copy": "^0.3.0",
    "node-sass": "^3.8.0"
  },
  "dependencies": {
    "glpb-slick-carousel": "^1.6.0"
  }
}
